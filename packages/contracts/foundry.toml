[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc = "0.8.28"
optimizer = true
optimizer_runs = 200
verbosity = 2

remappings = [
    "@openzeppelin/=lib/openzeppelin-contracts/",
]

[fuzz]
runs = 256
