/**
 * @cohortlens/shared — shared types and utilities for CohortLens.
 *
 * Domain types for the Capital Flow Graph (nodes, edges, Lens, Cohort, Route)
 * land here in Fase 1 (issue #8).
 */
export const APP_NAME = 'CohortLens';
